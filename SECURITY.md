# Repository data hygiene

Do not commit terminal IDs, merchant IDs, RRN values, authorization codes, card data, or production logs. All merchant-specific values must be supplied locally at deployment time.
